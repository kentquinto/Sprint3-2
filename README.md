This repository contains two small exercises demonstrating how to apply Singleton Pattern and Dependency Injection (DI) in practice
Folder "Tigger": The Singleton Pattern restricts object creation to a single instance and provides a global access point.
Folder "DIExercise": Instead of each object in a class developing its own dependencies, they are injected by the class constructor from an outside source.
